{
  "name": "vizyto-booking-widget",
  "private": true,
  "version": "1.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4500",
    "build:cdn": "vite build && node scripts/build-deploy.mjs",
    "deploy": "pnpm build:cdn && pnpm dlx wrangler@4 deploy"
  },
  "dependencies": {
    "preact": "^10.24.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.4",
    "typescript": "^5.9.3",
    "vite": "^7.3.0"
  }
}
