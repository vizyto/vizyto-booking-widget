{
  "name": "vizyto-booking-widget",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4500",
    "build:cdn": "vite build && node scripts/build-deploy.mjs",
    "deploy": "pnpm build:cdn && pnpm dlx wrangler@3 pages deploy deploy --project-name=vizyto-booking-widget --branch=main"
  },
  "dependencies": {
    "preact": "^10.24.3"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.4",
    "typescript": "^5.9.3",
    "vite": "^7.3.0"
  }
}
